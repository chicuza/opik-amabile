# Backend proxy configuration for Railway
# This file is separate from default.conf to avoid being processed by Opik's patch-nginx.conf.sh

upstream opik_backend {
    server backend:8080;
}

upstream opik_guardrails {
    server guardrails:5000;
}
